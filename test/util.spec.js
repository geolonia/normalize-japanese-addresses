const chai = require('chai');
const assert = chai.assert;

describe('Tests for `src/imi-enrichment-address/lib/util.js`.', () => {
  it('Get correct URL of Geolonia\'s style.', () => {
    assert.deepEqual(1, 0);
  });
})
